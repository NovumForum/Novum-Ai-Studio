## 2025-03-31 - Node Schema Tooltips and Descriptions
**Learning:** ComfyUI nodes display `description` and input/output parameter `tooltip` metadata in the UI node graph editor on hover, greatly improving accessibility and usability for users constructing complex workflows.
**Action:** When adding or updating node schemas, always ensure `description` and parameter `tooltip` strings are explicitly provided.
