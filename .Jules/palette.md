## 2026-03-01 - ColorToRGBInt Node Discovery & Schema Tooltips
**Learning:** ComfyNode extensions defined via `io.ComfyNode` and `io.Schema` support `search_aliases` and parameter `tooltip` fields, significantly improving node discoverability and contextual input format hints in the UI graph editor.
**Action:** Always include helpful search aliases (e.g. hex to rgb, color code) and explicit input format tooltips when defining or upgrading `io.ComfyNode` schemas.
