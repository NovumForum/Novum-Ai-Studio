## 2025-05-18 - Logic Node Schema Tooltips and Search Aliases
**Learning:** Adding explicit descriptions, search aliases (e.g. `if`, `branch`, `conditional`), and input/output parameter tooltips to schema definitions on core control flow/logic nodes greatly improves canvas discoverability and contextual inline user guidance without modifying execution behavior.
**Action:** Always include comprehensive parameter tooltips and common search terms when adding or documenting schema definitions for logic and utility nodes in `comfy_extras/`.
